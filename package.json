{
  "name": "speak",
  "version": "0.1.0",
  "description": "Ask Claude to read its answer out loud. MCP server plus Claude Code skill and hook.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "speak-mcp": "dist/server.js",
    "speak": "dist/cli.js"
  },
  "files": ["dist", "skills", "hooks", "README.md"],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "edge-tts-universal": "^1.4.0",
    "zod": "^3.24.0"
  },
  "optionalDependencies": {
    "kokoro-js": "^1.2.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
